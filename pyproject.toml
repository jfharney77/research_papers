[project]
name = "research-papers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "graphviz>=0.21",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "python-docx>=1.1",
    "typer>=0.12",
    "jinja2>=3.1",
    "python-multipart>=0.0.9",
]

[project.scripts]
papers = "docbuilder.cli:app"
