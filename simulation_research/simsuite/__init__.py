"""simsuite: shared infrastructure + CalibSoc (Project 1) + DivProbe (Project 4).

Implements the first two projects of implementation_specs.md in the sequencing
order the spec prescribes (1 -> 4 -> 2), on top of one shared trace schema and
model-access layer so later projects (AsymBench, TieredSim, ...) can score any
run produced here.
"""

__version__ = "0.1.0"
